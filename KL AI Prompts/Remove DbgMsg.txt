#codebase
In all *.php files in this workspace except on the files ConnectDB*.php, ArchiveConnectDB.php and OCOpenCartConnectDB.php:


1) Delete all lines defining the variable $DbgMsg (lines starting as $DbgMsg= or $DbgMsg = ).
2) Update all calls to function DB_query(), setting the $DbgMsg as '', if the call has an explicit parameter $DbgMsg.
3) Update all calls to function DB_query_oc(), setting the $DbgMsg as '', if the call has an explicit parameter $DbgMsg.
4) Update all calls to function DB_query_archive(), setting the $DbgMsg as '', if the call has an explicit parameter $DbgMsg.
5) Iterate all *.php files until done.
6) Do NOT update, format, improve, etc anything else on the code except related to the $DbgMsg variable

