1) Replace the names of the following variable names 

$sql replace to $SQL
$result replace to $Result
$myrow replace to $MyRow
$Myrow replace to $MyRow
$myRow replace to $MyRow
$msg replace to $Msg
$StockId replace to $StockID
$stockid replace to $StockID
$Stockid replace to $StockID
$debtorsmasterResult replace to $DebtorsMasterResult
$custitemResult replace to $CustItemResult 
$Userresult replace to $UserResult
$userresult replace to $UserResult
$filename replace to $FileName
$ext replace to $Ext
$message replace to $Message
$oldfile replace to $OldFile
$tempid replace to $TempID
$imagefile replace to $ImageFile
$debug replace to $Debug


General propmt rules: 
1) Do not cut long lines. 
2) Do not delete any comment in the code
3) Do not change, modify, upgrade any other part of the code not related to the propmt.
4) Apply all changes above directly to the file in the editor.
