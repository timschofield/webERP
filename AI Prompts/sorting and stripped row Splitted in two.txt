In all the functions defined in this file in my main editor tab:

1) Modify the HTML tags where needed, so all tables on a form have this structure:
<table>
	<thead>
		<th></th>
        ................
	</thead>
	<tbody>
		<td></td>
		................
    </tbody>
</table>

2) below the while statement, there will be an echo </table> statemnet. Complete this echo statements to add the </div> and </form> if needed

3) Do not cut long lines. 

4) Apply these changes directly to the file in my editor.




In all the functions defined in this file in my main editor tab:

1) Replace <th class="ascending"> with <th class="SortedColumn">. The <th> tags without class="ascending" do not need to be replaced.

2) If there is a line $k = StartEvenOrOddRow($k);

	2.1) Delete the lines: $k = 0; //row colour counter

	2.2) Delete the lines: $k = StartEvenOrOddRow($k);

	2.3) In the printf or echo statements below it (inside or below the while structure) add or modify the <tr> tag into <tr class="striped_row">
	
	3.4) In the printf or echo statements below the while structure add or modify the <tr> tag into <tr class="striped_row">

3) Do not cut long lines. 

4) Apply these changes directly to the file in my editor.

