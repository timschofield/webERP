
# KL settings 
*.ini
# ignore the part_pics folders
companies/test_erp/part_pics/*
companies/kl_erp/part_pics/*

# ignore the cache files and the log
includes/WebClientPrint/wcpcahe/
companies/test_erp/logs/weberp.log

# KL settings END

# Ignore NetBeans project metadata
nbproject/
# As well as Jetbrains stuff
/.idea/
# And VSCode
/.vscode/launch.json
# And random assorted tooling files
Thumbs.db

# Only config.distrib.php is to be distributed
# In PTADU scenario, the credentials are stored in another file (KLConfig/KLCredentials.php) under repository PTADU-webERP-Secrets)
# /config.php 

# Ignore everything in the companies directory...
/companies/*
# ...except for weberpdemo, but KL do not want weberpdemo anyway, we just want the kl_erp...
#!/companies/weberpdemo
!/companies/kl_erp

# the per-developer phpunit config and phpunit cache
/.phpunit.result.cache
/phpunit.xml

# And the eventual .git dir from composer-installed projects which are downloaded as git clones instead of zipballs
/vendor/phplot/phplot/.git/

# KL settings

# KL settings END
